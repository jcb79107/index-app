import SwiftUI

// This file is intentionally left as the “legacy / mock” detail screen.
// The live (remote) screen is PlayerDetailViewRemote.swift.

struct PlayerDetailView: View {
    var body: some View {
        Text("Legacy PlayerDetailView (unused)")
            .foregroundStyle(.secondary)
    }
}

#Preview {
    PlayerDetailView()
}
